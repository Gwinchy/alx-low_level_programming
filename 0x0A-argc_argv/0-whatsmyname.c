#include <stdio.h>
#include "main.h"
/**
 * main - Starting point
 *
 * @argc: argument count
 *
 * @argv: pointer to argc array
 *
 * Return: Always 0 (Success)
 */
int main(int argc, char *argv[])
{
	if (argc > 1)
	{
		_putchar(argv[1][0];);
	}
	return (0);
}
