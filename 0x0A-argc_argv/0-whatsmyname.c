#include <stdio.h>
/**
 * main - Starting point
 *
 * @argc: argument count
 *
 * @argv: pointer to argc array
 *
 * Return: Always 0 (Success)
 */
int main(int argc, char *argv[])
{
	printf("%s/n", argv[0]);
	return (0);
}
