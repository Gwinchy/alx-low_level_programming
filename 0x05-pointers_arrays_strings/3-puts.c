#include "main.h"
#include <stdio.h>
/**
 * _puts- function that prints a string to the standard output
 * @str: string to be printed by the function
 * Return: void
 */
void _puts(char *str)
{
	putchar(*str);
	putchar('\n');
}
