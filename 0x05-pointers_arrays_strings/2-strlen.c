#include "main.h"
#include <string.h>
/**
 * _strlen- returns the length of a string
 * @s: string pointer being investigated
 * Return: void
 */
int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
}
