#include "main.h"
/**
 * mul(int a, int b)- function that multiplies two integers a and b
 * @a : first integer
 * @b : second integer
 * Return: value of the result
 */
int mul(int a, int b)
{
	int result = a * b;
	{
		return (result);
	}
}
