#include "main.h"
/**
 * _isupper - checks if a character is uppue or lower case
 * @c : integer or character being investigated
 * Return: 1 if upper and 0 if is not
 */
int _isupper(int c)
{
	if (_isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
