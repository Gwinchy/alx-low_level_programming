#include "main.h"
/**
 * _isdigit - checks if c is an integer or not
 * @c : digit or integer to be tested
 * Return: 1 if true and 0 if false
 */
int _isdigit(int c)
{
	if (_isdigit(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
